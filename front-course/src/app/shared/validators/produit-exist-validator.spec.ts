import {ProduitExistValidator} from './produit-exist-validator';

describe('ProduitExistValidator', () => {
  it('should create an instance', () => {
    expect(new ProduitExistValidator()).toBeTruthy();
  });
});
