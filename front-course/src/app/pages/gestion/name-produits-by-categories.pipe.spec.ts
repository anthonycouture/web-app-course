import {NameProduitsByCategoriesPipe} from './name-produits-by-categories.pipe';

describe('NameProduitsByCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new NameProduitsByCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
