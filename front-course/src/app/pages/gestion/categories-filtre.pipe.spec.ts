import {CategoriesFiltrePipe} from './categories-filtre.pipe';

describe('CategoriesFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
